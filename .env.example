# Server settings
HOST=0.0.0.0
PORT=8000

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# File upload limits
MAX_FILE_SIZE_MB=10

# Default model (rembg or withoutbg)
DEFAULT_MODEL=rembg
